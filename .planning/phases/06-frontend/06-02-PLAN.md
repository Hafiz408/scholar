---
phase: 06-frontend
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - frontend/src/components/GoalForm.tsx
  - frontend/src/components/StudyPlan.tsx
  - frontend/src/components/ProgressBar.tsx
  - frontend/src/app/goals/new/page.tsx
  - frontend/src/app/goals/[id]/page.tsx
autonomous: true
requirements:
  - FE-02
  - FE-03

must_haves:
  truths:
    - "User can fill the goal creation form (title, topic, source multi-select, deadline, level, sessions/week) and submit"
    - "After submission the user is navigated to the goal detail page"
    - "Goal detail page shows a progress bar derived from completed vs total sessions"
    - "Session cards display locked/available/in-progress/complete state with correct visual treatment"
    - "Complete sessions show a quiz score badge"
  artifacts:
    - path: "frontend/src/components/GoalForm.tsx"
      provides: "Controlled goal creation form calling createGoal from api.ts"
      min_lines: 80
    - path: "frontend/src/components/StudyPlan.tsx"
      provides: "Session card list rendering all sessions with state-derived styles"
      min_lines: 60
    - path: "frontend/src/components/ProgressBar.tsx"
      provides: "Progress bar showing completed/total sessions"
      min_lines: 20
    - path: "frontend/src/app/goals/new/page.tsx"
      provides: "Goal creation page rendering GoalForm"
      min_lines: 20
    - path: "frontend/src/app/goals/[id]/page.tsx"
      provides: "Goal detail page rendering ProgressBar + StudyPlan"
      min_lines: 50
  key_links:
    - from: "frontend/src/app/goals/new/page.tsx"
      to: "frontend/src/components/GoalForm.tsx"
      via: "import and render"
      pattern: "GoalForm"
    - from: "frontend/src/components/GoalForm.tsx"
      to: "frontend/src/lib/api.ts"
      via: "createGoal, listSources"
      pattern: "import.*api"
    - from: "frontend/src/app/goals/[id]/page.tsx"
      to: "frontend/src/lib/api.ts"
      via: "getGoalPlan"
      pattern: "getGoalPlan"
    - from: "frontend/src/components/StudyPlan.tsx"
      to: "frontend/src/app/study/[sessionId]/page.tsx"
      via: "Link to session on available/in-progress cards"
      pattern: "href.*study"
---

<objective>
Build the goal creation form (FE-02) and goal detail page (FE-03). Users fill a form to create a study goal, are redirected to the goal detail page, and see a progress bar and session card list with locked/available/in-progress/complete states and quiz score badges.

Purpose: Closes the goal and planning loop — users can create goals and track session progress before starting sessions.
Output: GoalForm, StudyPlan, ProgressBar components + goals/new and goals/[id] pages.
</objective>

<execution_context>
@/Users/mohammedhafiz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mohammedhafiz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-frontend/06-RESEARCH.md
@.planning/phases/06-frontend/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Goal creation form component and page</name>
  <files>frontend/src/components/GoalForm.tsx, frontend/src/app/goals/new/page.tsx</files>
  <action>
    Implement `frontend/src/components/GoalForm.tsx` as a `'use client'` component.

    State:
    - `title: string` — goal title
    - `topic: string` — study topic
    - `selectedSourceIds: string[]` — multi-select of knowledge source IDs
    - `deadlineDays: number` — deadline in days (options: 7, 14, 21, 30, 60, 90)
    - `level: string` — study level ('beginner', 'intermediate', 'advanced')
    - `sessionsPerWeek: number` — sessions per week (1–7)
    - `sources: KnowledgeSource[]` — fetched on mount via `listSources()`
    - `submitting: boolean` — loading state

    On mount: call `listSources()` and populate `sources`.

    Form fields:
    1. Title — `<input type="text" required>` bound to `title`
    2. Topic — `<input type="text" required>` bound to `topic`
    3. Knowledge Sources multi-select — render each source as a `<label><input type="checkbox" value={source.id}>` checked state managed in `selectedSourceIds`. Toggle: if id in array remove it, else add it. Use `Array.from` pattern — do NOT use HTML `<select multiple>` (causes INGEST-06 422 validation bug described in research pitfall 4).
    4. Deadline — `<select>` with options for 7/14/21/30/60/90 days bound to `deadlineDays`
    5. Level — three `<input type="radio">` buttons for 'beginner', 'intermediate', 'advanced' bound to `level`
    6. Sessions/week — `<input type="number" min="1" max="7">` bound to `sessionsPerWeek`

    Submit handler (`handleSubmit`):
    - Call `createGoal({ title, topic, knowledge_source_ids: selectedSourceIds, deadline_days: deadlineDays, level, sessions_per_week: sessionsPerWeek })`
    - On success: use `useRouter` from `next/navigation` to push to `/goals/${studyPlan.goal_id}`
    - On error: display error message in UI (do not crash)
    - Set `submitting` true during request, reset on error

    Tailwind styling: form layout `space-y-4`, input `border rounded p-2 w-full`, submit button `bg-blue-600 text-white px-4 py-2 rounded disabled:opacity-50`.

    Implement `frontend/src/app/goals/new/page.tsx` — server component that renders `<GoalForm />` with a "Create Study Goal" heading.
  </action>
  <verify>
    Run `cd /Users/mohammedhafiz/Desktop/Personal/scholar/frontend && npx tsc --noEmit 2>&1` — zero errors. Confirm GoalForm.tsx has `'use client'` directive, imports from `@/lib/api` and `@/types`, uses checkbox pattern for source multi-select.
  </verify>
  <done>
    GoalForm renders with all 6 fields. Checkbox-based source multi-select builds a `string[]` correctly. Submitting calls createGoal and redirects to /goals/{id} on success. TypeScript compiles clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Goal detail page — progress bar and session cards</name>
  <files>frontend/src/components/ProgressBar.tsx, frontend/src/components/StudyPlan.tsx, frontend/src/app/goals/[id]/page.tsx</files>
  <action>
    Implement `frontend/src/components/ProgressBar.tsx` as a simple component (can be server or client):
    - Props: `completed: number`, `total: number`
    - Render a `<div>` progress track with an inner `<div>` width set to `${Math.round((completed / total) * 100)}%`
    - Show text label: "{completed} of {total} sessions complete"
    - Tailwind: outer `bg-gray-200 rounded-full h-2`, inner `bg-blue-600 h-2 rounded-full transition-all`

    Implement `frontend/src/components/StudyPlan.tsx` as a `'use client'` component:
    - Props: `sessions: StudySession[]`
    - For each session, derive state using this logic (import from research Pattern 6):
      ```
      function getSessionState(session, prevSession) {
        if (session.status === 'complete') return 'complete'
        if (session.status === 'in_progress') return 'in_progress'
        if (session.session_number === 1) return 'available'
        if (prevSession?.status === 'complete') return 'available'
        return 'locked'
      }
      ```
    - Render each session as a card with: session number badge, title, topic, estimated_minutes label, state pill.
    - State-based styles:
      - locked: `opacity-50 cursor-not-allowed border-gray-200`
      - available: `border-blue-300 cursor-pointer hover:bg-blue-50`
      - in_progress: `border-yellow-400 bg-yellow-50`
      - complete: `border-green-400 bg-green-50`
    - For 'available' and 'in_progress' states: wrap card in `<Link href={'/study/' + session.id}>` from next/link
    - For 'complete' state: show quiz score badge if `session.quiz_score != null`: `<span>{Math.round(session.quiz_score * 100)}%</span>` in green badge
    - For 'locked' state: show a lock icon (simple emoji "🔒" or SVG) — no link

    Implement `frontend/src/app/goals/[id]/page.tsx` as a `'use client'` component:
    - Get `goalId = params.id` (Next.js 14 — params is synchronous, NOT a Promise)
    - On mount: call `getGoalPlan(goalId)`, set `plan: StudyPlan | null` state
    - Render: goal title + topic as heading, ProgressBar with completed/total, StudyPlan with sessions array
    - Derive progress: count sessions where status === 'complete' for `completed`, use `plan.sessions.length` for `total`
    - Show loading state while plan is null
    - Show error state if fetch fails
  </action>
  <verify>
    Run `cd /Users/mohammedhafiz/Desktop/Personal/scholar/frontend && npx tsc --noEmit 2>&1` — zero errors. Verify goal detail page uses `params.id` directly (not await params), and StudyPlan uses Link only for available/in_progress sessions.
  </verify>
  <done>
    ProgressBar renders correct width percentage. StudyPlan renders session cards with correct state-derived styles. Complete sessions show quiz score badge. Available/in-progress sessions link to /study/{sessionId}. TypeScript compiles clean.
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` produces zero errors
2. GoalForm.tsx has 'use client', uses checkbox multi-select (not HTML select multiple)
3. StudyPlan.tsx getSessionState function matches the logic from research
4. goals/[id]/page.tsx uses `params.id` synchronously (Next.js 14 pattern)
5. ProgressBar width formula: `Math.round((completed / total) * 100)` as percentage string
</verification>

<success_criteria>
- Goal creation form: 6 fields, checkbox multi-select for sources, submits to createGoal, redirects on success
- Progress bar: correct percentage, smooth transition CSS
- Session cards: 4 states with distinct Tailwind styles, links only for available/in-progress
- Quiz score badge on complete sessions
- TypeScript: zero compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend/06-02-SUMMARY.md` following the summary template.
</output>
